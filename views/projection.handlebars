<h4>Projection</h4>
<div class='container'>
        <div class="row">
            <h3>Insert Projection</h3>
        </div>
        <div class='row'>
            {{!-- dropdown for partner select --}}
        </div>
        <div class="row">
          <div class='col s12'>  
            <table>
                <thead>
                    <tr>
                        {{#each header}}
                            {{#each this}}
                                <th>
                                    {{.}}
                                </th>
                            {{/each}}
                        {{/each}}
                    </tr>
                </thead>
                <tbody>
                    {{#each week}}
                    <tr>
                        {{#each this}}
                        <td>
                            {{.}}
                        </td>
                        {{/each}}
                    </tr>
                    {{/each}}
                </tbody>
            </table>
          </div>
        </div> 
        <div class='row'>
            {{#if admin}}
            <!--buttons for pick a date and duration-->
            {{/if}}
        </div>
        

</div>


<script>

    function validateForm() {
        var x = $('#start_date').val();
        if (x == "not a monday") {
            // something here to ask if they're sure, or to just flat out ignore it
            return false;
        }
    }

</script>