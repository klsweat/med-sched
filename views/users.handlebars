<div class="row page-label">
    <div class="col s12 l8 offset-l2">
        <h4>Update Users</h4>
        {{#each User}}
            {{User.First_Name}} {{User.First_Name}}
        {{/each}}
    </div>
</div>